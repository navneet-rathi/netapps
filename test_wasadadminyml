---
- name: Testing the switch user
  hosts: dc_web:dr_web
  tasks:
    - name: run
      ansible.builtin.shell: "whoami"

    - name: run
      ansible.builtin.shell: whoami
      become_user: wasadmin
